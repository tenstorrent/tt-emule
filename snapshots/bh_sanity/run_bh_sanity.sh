#!/usr/bin/env bash
# AUTOGENERATED by snapshots/bh_sanity/generate.py — do not edit.
#
# Identical-invocation invariant: every test path, every original env var,
# and every pytest arg from tt-metal/tests/pipeline_reorg/ttnn-tests.yaml
# is preserved verbatim below. The diff vs the source cmd consists ONLY of
# the following additive layer: emule env (TT_METAL_EMULE_MODE, TT_METAL_SLOW_DISPATCH_MODE),
# variant env (bh_emule: MESH_DEVICE, TT_METAL_MOCK_CLUSTER_DESC_PATH), the `timeout`
# wrapper, the --junitxml= flag, the stdout/stderr redirection.
#
# Plus three status-snapshot-mode transformations that the audit allows:
#
#   1. `-x` (stop-on-first-failure) is STRIPPED. Without this, the first
#      failure per entry truncates the rest of the entry — distorting the
#      `% passing` headline metric the snapshot is built to report.
#
#   2. `--forked` is INJECTED (per-test process isolation). Without this,
#      a single segfault crashes the whole pytest process and the rest of
#      the entry doesn't run — same shape of distortion as `-x`.
#
#   3. `--continue-on-collection-errors` is INJECTED. Without this, any
#      one file with an ImportError (e.g. transitively importing a
#      package not in the toolchain venv) aborts the entire test session
#      via `!!! Interrupted: N errors during collection !!!`. With it,
#      the offending file becomes a single `<error>` in the JUnit XML
#      and the rest of the directory's tests run normally. The parser
#      surfaces these files explicitly as excluded-files.
#
# All three transformations widen the test coverage of a single entry's run;
# none alters the test set itself or how individual tests execute.
#
# Re-derive with: snapshots/bh_sanity/generate.py

set -uo pipefail

: "${CLUSTER_EXAMPLES:?CLUSTER_EXAMPLES must point at cluster_descriptor_examples}"
: "${TT_METAL_HOME:?TT_METAL_HOME must be set}"
OUT="/localdev/arminale/tt-emule/snapshots/bh_sanity/bh_emule"
mkdir -p "$OUT"

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-data-movement-group" ]; then
  echo "::group::ttnn data movement group"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn data movement group"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 900 pytest --continue-on-collection-errors --forked --timeout 60 -v tests/ttnn/unit_tests/operations/data_movement/test_tilize.py::test_tilize_test tests/ttnn/unit_tests/operations/data_movement/test_tilize_with_val_padding.py::test_run_tilize_with_val_padding_test tests/ttnn/unit_tests/operations/data_movement/test_untilize.py::test_untilize_single_core_interleaved_to_interleaved tests/ttnn/unit_tests/operations/data_movement/test_untilize_with_unpadding.py::test_untilize_with_unpadding_height_sharded tests/ttnn/unit_tests/operations/data_movement/test_pad.py::test_pad tests/ttnn/unit_tests/operations/data_movement/test_fill_pad.py::test_fill_pad_complex_sharding tests/ttnn/unit_tests/operations/data_movement/test_concat.py::test_tiled_concat tests/ttnn/unit_tests/operations/data_movement/test_permute.py::test_permute_identity tests/ttnn/unit_tests/operations/data_movement/test_slice.py::test_slice_usecase1 tests/ttnn/unit_tests/operations/data_movement/test_repeat.py::test_pc_with_different_shapes_in_sequence tests/ttnn/unit_tests/operations/data_movement/test_embedding.py::test_base_case tests/ttnn/unit_tests/operations/data_movement/test_backward_embedding.py::test_embedding_bw_with_program_cache tests/ttnn/unit_tests/operations/data_movement/test_gather.py::test_gather_general tests/ttnn/unit_tests/operations/data_movement/test_tosa_scatter.py::test_tosa_scatter_normal tests/ttnn/unit_tests/operations/data_movement/test_clone.py::test_clone_callback tests/ttnn/unit_tests/operations/data_movement/test_sort.py::test_sort_indices tests/ttnn/unit_tests/operations/data_movement/test_interleaved_to_sharded.py::test_interleaved_to_sharded_nd_with_equivalent_2d tests/ttnn/unit_tests/operations/data_movement/test_sharded_to_interleaved_oob.py::test_s2i_dram_height_sharded tests/ttnn/unit_tests/operations/data_movement/test_convert_to_hwc.py::test_convert_to_hwc_dram_uneven_sharding tests/ttnn/unit_tests/operations/data_movement/test_full.py::test_full_int tests/ttnn/unit_tests/operations/data_movement/test_dropout.py::test_dopout tests/ttnn/unit_tests/operations/data_movement/test_non_zero_indices.py::test_non_zero_indices_ttnn --junitxml=$OUT/ttnn-data-movement-group.xml > $OUT/ttnn-data-movement-group.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn data movement group)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-eltwise-group-1" ]; then
  echo "::group::ttnn eltwise group 1"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn eltwise group 1"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/eltwise -v --splits 4 --group 1 -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-eltwise-group-1.xml > $OUT/ttnn-eltwise-group-1.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn eltwise group 1)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-eltwise-group-2" ]; then
  echo "::group::ttnn eltwise group 2"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn eltwise group 2"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/eltwise -v --splits 4 --group 2 -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-eltwise-group-2.xml > $OUT/ttnn-eltwise-group-2.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn eltwise group 2)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-eltwise-group-3" ]; then
  echo "::group::ttnn eltwise group 3"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn eltwise group 3"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/eltwise -v --splits 4 --group 3 -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-eltwise-group-3.xml > $OUT/ttnn-eltwise-group-3.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn eltwise group 3)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-eltwise-group-4" ]; then
  echo "::group::ttnn eltwise group 4"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn eltwise group 4"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/eltwise -v --splits 4 --group 4 -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-eltwise-group-4.xml > $OUT/ttnn-eltwise-group-4.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn eltwise group 4)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-conv-group" ]; then
  echo "::group::ttnn conv group"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn conv group"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/conv -v -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-conv-group.xml > $OUT/ttnn-conv-group.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn conv group)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-pool-group" ]; then
  echo "::group::ttnn pool group"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn pool group"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/pool -v -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-pool-group.xml > $OUT/ttnn-pool-group.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn pool group)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-matmul-group" ]; then
  echo "::group::ttnn matmul group"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn matmul group"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/matmul -v -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-matmul-group.xml > $OUT/ttnn-matmul-group.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn matmul group)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-fused-group-1" ]; then
  echo "::group::ttnn fused group 1"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn fused group 1"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/fused -v --splits 2 --group 1 --splitting-algorithm least_duration -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-fused-group-1.xml > $OUT/ttnn-fused-group-1.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn fused group 1)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-fused-group-2" ]; then
  echo "::group::ttnn fused group 2"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn fused group 2"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/fused -v --splits 2 --group 2 --splitting-algorithm least_duration -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-fused-group-2.xml > $OUT/ttnn-fused-group-2.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn fused group 2)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-misc-ops-group" ]; then
  echo "::group::ttnn misc ops group"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn misc ops group"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/debug tests/ttnn/unit_tests/operations/point_to_point tests/ttnn/unit_tests/operations/transformers -v -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-misc-ops-group.xml > $OUT/ttnn-misc-ops-group.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn misc ops group)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-sdpa-group" ]; then
  echo "::group::ttnn sdpa group"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn sdpa group"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 2700 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/sdpa -v -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-sdpa-group.xml > $OUT/ttnn-sdpa-group.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn sdpa group)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "ttnn-reduce-group" ]; then
  echo "::group::ttnn reduce group"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  ttnn reduce group"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/operations/reduce -v -m 'not disable_fast_runtime_mode' --junitxml=$OUT/ttnn-reduce-group.xml > $OUT/ttnn-reduce-group.log 2>&1
  rc=$?; echo "rc=$rc (entry=ttnn reduce group)"
  echo "::endgroup::"
fi

if [ -z "${ONLY:-}" ] || [ "${ONLY}" = "core-ttnn-unit-test-group" ]; then
  echo "::group::core ttnn unit test group"
  echo "# source: /localdev/arminale/tt-metal/tests/pipeline_reorg/ttnn-tests.yaml"
  echo "# entry:  core ttnn unit test group"
  TT_METAL_EMULE_MODE=1 TT_METAL_SLOW_DISPATCH_MODE=1 MESH_DEVICE=P100 TT_METAL_MOCK_CLUSTER_DESC_PATH=$CLUSTER_EXAMPLES/blackhole_P100.yaml timeout 5400 pytest --continue-on-collection-errors --forked --timeout 300 tests/ttnn/unit_tests/base_functionality tests/ttnn/unit_tests/benchmarks tests/ttnn/unit_tests/tensor -v -m 'not disable_fast_runtime_mode' '&&' pytest --timeout 300 tests/ttnn/unit_tests/per_core_allocation -v --junitxml=$OUT/core-ttnn-unit-test-group.xml > $OUT/core-ttnn-unit-test-group.log 2>&1
  rc=$?; echo "rc=$rc (entry=core ttnn unit test group)"
  echo "::endgroup::"
fi

